@startuml "Discipline Management Activity"
skinparam defaultFontSize 22
skinparam defaultFontName "Times New Roman"

|Ban quản lý|
start
:Mở quản lý kỷ luật;

|Hệ thống|
:Hiển thị danh sách phiếu vi phạm;

|Ban quản lý|
if (Chọn tab?) then (Phiếu vi phạm)
  :Tạo/Sửa/Xóa phiếu vi phạm;
  |Hệ thống|
  :Tính điểm trừ, lưu phiếu\ncập nhật điểm rèn luyện;
  if (Đạt ngưỡng cảnh báo?) then (Có)
    :Gửi email cảnh báo;
  else (Không)
  endif
elseif (Chọn tab?) then (Bảng quy định)
  :Thêm/Sửa quy định vi phạm;
  |Hệ thống|
  :Lưu quy định, ghi log;
else (Thống kê)
  |Hệ thống|
  :Hiển thị thống kê vi phạm\nvà danh sách cảnh báo;
  |Ban quản lý|
endif

:Nhận kết quả;
stop
@enduml
